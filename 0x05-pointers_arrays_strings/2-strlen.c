#include "main.h"
#include <stdio.h>

/**
 * main - is my main function
 * Return - is a return
 */
int main(void)
{
	char *str;
	int len;

	str = "bad";
	len = _strlen(str);
	printf("%d\n", len);
	printf("%s\n", str);
}

int _strlen(char *s)
{
	char a[] = 
	int i;
	i = sizeof()	
}
