CC = gcc
SRC = main.c school.c
OBJ = $(SRC:%.c=%.o)
NAME = school
RM = re

.PHONY := all clean oclean fclean re

all: ${NAME}

${NAME}: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

%.o: %.c m.h
	${CC} -c -o $@ $<

clean:
	rm -f *~ ${NAME}

oclean:
	rm -f ${OBJ}

fclean:
	rm -f *~ ${NAME}
	rm -f ${OBJ}

re:
	rm -f ${OBJ}
	${CC} -c ${SRC}
	${CC} ${OBJ} -o ${NAME}
