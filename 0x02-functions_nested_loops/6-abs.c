#include "main.h"

/**
 * _abs - is a functio to return an absolute value
 * @i: is the param to return the absolute value for
 * Return: is to return the absolute value
 */
int _abs(int i)
{
	if (i < 0)
		return (-i);
	else
		return (i);
}
