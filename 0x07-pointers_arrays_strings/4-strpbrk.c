#include "main.h"
/**
 * _strpbrk - i
 * @s: a
 * @accept: is an accept
 *
 * Return: is a return
 */
char *_strpbrk(char *s, char *accept)
{
	int i, n;

	i = n = 0;
	if (s[0] != '\0')
		i++;
	if (accept[1] != '\0')
		n++;

	return (s);
}
